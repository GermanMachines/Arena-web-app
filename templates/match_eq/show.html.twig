{# 
{% extends 'base.html.twig' %}

{% block title %}MatchEq{% endblock %}

{% block body %}
    <h1>MatchEq</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idmatch</th>
                <td>{{ match_eq.idmatch }}</td>
            </tr>
            <tr>
                <th>Idequipe</th>
                <td>{{ match_eq.idequipe }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ match_eq.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_match_eq_index') }}">back to list</a>

    <a href="{{ path('app_match_eq_edit', {'idmatch': match_eq.idmatch}) }}">edit</a>

    {{ include('match_eq/_delete_form.html.twig') }}
{% endblock %}
#}

 {% extends 'base-admin.html.twig' %}
{% block body %}
    <h1>MatchEq</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idmatch</th>
                <td>{{ match_eq.idmatch }}</td>
            </tr>
            <tr>
                <th>Idequipe</th>
                <td>{{ match_eq.idequipe }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ match_eq.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_match_eq_index') }}">back to list</a>

    <a href="{{ path('app_match_eq_edit', {'idmatch': match_eq.idmatch}) }}">edit</a>

    {{ include('match_eq/_delete_form.html.twig') }}
{% endblock %}