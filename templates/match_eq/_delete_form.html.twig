<form method="post" action="{{ path('app_match_eq_delete', {'idmatch': match_eq.idmatch}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match_eq.idmatch) }}">
    <button class="btn">Delete</button>
</form>
