{#{% extends 'base.html.twig' %}

{% block title %}Tournois index{% endblock %}

{% block body %}
    <h1>Tournois index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idtournois</th>
                <th>Titre</th>
                <th>DateDebut</th>
                <th>DateFin</th>
                <th>Descriptiontournois</th>
                <th>Type</th>
                <th>Nbrparticipants</th>
                <th>Idjeux</th>
                <th>Winner</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tournoi in tournois %}
            <tr>
                <td>{{ tournoi.idtournois }}</td>
                <td>{{ tournoi.titre }}</td>
                <td>{{ tournoi.dateDebut ? tournoi.dateDebut|date('Y-m-d') : '' }}</td>
                <td>{{ tournoi.dateFin ? tournoi.dateFin|date('Y-m-d') : '' }}</td>
                <td>{{ tournoi.descriptiontournois }}</td>
                <td>{{ tournoi.type }}</td>
                <td>{{ tournoi.nbrparticipants }}</td>
                <td>{{ tournoi.idjeux }}</td>
                <td>{{ tournoi.winner }}</td>
                <td>{{ tournoi.status }}</td>
                <td>
                    <a href="{{ path('app_tournois_show', {'idtournois': tournoi.idtournois}) }}">show</a>
                    <a href="{{ path('app_tournois_edit', {'idtournois': tournoi.idtournois}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tournois_new') }}">Create new</a>
{% endblock %}

#}
 {% extends 'base-admin.html.twig' %}
{% block body %}
 <section class="ftco-section">
		<div class="container">
			<div class="row justify-content-center">
				<div class="col-md-6 text-center mb-5">
				</div>
			</div>
			<div class="row">
				<div class="col-md-12">
					<div class="table-wrap">



    <table class="table table-bordered border-primary">
         <thead class="thead-dark">
            <tr>
                <th>Idtournois</th>
                <th>Titre</th>
                <th>DateDebut</th>
                <th>DateFin</th>
                <th>Descriptiontournois</th>
                <th>Type</th>
                <th>Nbrparticipants</th>
                <th>NomJeux</th>
                <th>Winner</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tournoi in tournois %}
            <tr>
                <td>{{ tournoi.idtournois }}</td>
                <td>{{ tournoi.titre }}</td>
                <td>{{ tournoi.dateDebut ? tournoi.dateDebut|date('Y-m-d') : '' }}</td>
                <td>{{ tournoi.dateFin ? tournoi.dateFin|date('Y-m-d') : '' }}</td>
                <td>{{ tournoi.descriptiontournois }}</td>
                <td>{{ tournoi.type }}</td>
                <td>{{ tournoi.nbrparticipants }}</td>
                <td>{{ tournoi.idjeux.nomjeux }}</td>
                <td>{{ tournoi.winner }}</td>
                <td>{{ tournoi.status }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('app_tournois_show', {'idtournois': tournoi.idtournois}) }}">show</a>
                    <a class="btn btn-success" href="{{ path('app_tournois_edit', {'idtournois': tournoi.idtournois}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tournois_new') }}">Create new</a>
    			</div>
				</div>
			</div>
		</div>
	</section>
{% endblock %}

