<form method="post" action="{{ path('app_tournois_delete', {'idtournois': tournoi.idtournois}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournoi.idtournois) }}">
    <button class="btn">Delete</button>
</form>
