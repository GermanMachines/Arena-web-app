{% extends 'base-admin.html.twig' %}

{% block title %}Categoryreclamation{% endblock %}

{% block body %}
    <h1>Categoryreclamation</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Nom</th>
                <td>{{ categoryreclamation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categoryreclamation_index') }}">back to list</a>

    <a href="{{ path('app_categoryreclamation_edit', {'id': categoryreclamation.id}) }}">edit</a>

    {{ include('categoryreclamation/_delete_form.html.twig') }}
{% endblock %}
