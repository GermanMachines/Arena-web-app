{#
{% extends 'base.html.twig' %}

{% block title %}Participation index{% endblock %}

{% block body %}
    <h1>Participation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idequipe</th>
                <th>Idtournois</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participation in participations %}
            <tr>
                <td>{{ participation.idequipe }}</td>
                <td>{{ participation.idtournois }}</td>
                <td>
                    <a href="{{ path('app_participation_show', {'idequipe': participation.idequipe}) }}">show</a>
                    <a href="{{ path('app_participation_edit', {'idequipe': participation.idequipe}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_participation_new') }}">Create new</a>
{% endblock %}
#}
 {% extends 'base-admin.html.twig' %}

{% block title %}Participation index{% endblock %}

{% block body %}
    <h1>Participation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Idequipe</th>
                <th>TitreTournois</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for participation in participations %}
            <tr>
                <td>{{ participation.idequipe }}</td>
                <td>{{ participation.idtournois }}</td>
                <td>
                    <a href="{{ path('app_participation_show', {'idequipe': participation.idequipe}) }}">show</a>
                    <a href="{{ path('app_participation_edit', {'idequipe': participation.idequipe}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_participation_new') }}">Create new</a>
{% endblock %}