    {# {{ form_start(form,{attr:{novalidate:'novalide'}}) }}
        {{ form_row(form.score) }}
        {{ form_row(form.commentaire) }}
        {{ form_row(form.idutulisateur) }}
        {{ form_row(form.idjeux) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }} #}


{# {{ form_start(form,{attr:{novalidate:'novalide'}}) }}
    {{ form_row(form.score) }}
    {{ form_row(form.commentaire) }}
    {{ form_row(form.idutulisateur) }}
    {{ form_row(form.idjeux) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
<main id="main" class="main">
    <section class="section">
        <div class="row">
            <div class="col-lg-6">
                <div class="card">
                    <div class="card-body">
                   
                        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}

                        <table>

                         <tr>
                                <td>
                            <h1> {{jeu.nomjeux}} </h1>
                                </td>
                            <tr>
                                <td>
                            <img src="{{ asset('uploads/' ~ jeu.imagejeux) }}" alt="{{jeu.nomjeux}}" width="100" height="100">
                                </td>
                    </tr>

                            <tr>
                               
                                <td>
                                     {{ form_widget(form.score, {'attr': {'class': 'rating','id':'input-id'}}) }}
                                </td>
                                <td>{{ form_errors(form.score,{'attr':{'class':'text-danger'}}) }}</td>
                            </tr>
                           <tr>
                        
                                
                                <td>
                                    {{ form_widget(form.commentaire) }}
                                </td>
                                <td>{{ form_errors(form.commentaire,{'attr':{'class':'text-danger'}}) }}</td>
                            </tr>
                         
                      

                           <tr style="visibility: hidden;">
                                <td class="hide">
                                    {{ form_label(form.idjeux,"jeux : ") }}
                                </td><td>
                                    {{ form_widget(form.idjeux) }}
                                </td>
                                <td>{{ form_errors(form.idjeux,{'attr':{'class':'text-danger'}}) }}</td>
                            </tr>    
                    

                           

                        </table>
                         <button class="btn btn-primary">{{ button_label|default('Rate') }}</button>
                        {{ form_end(form) }}
                    </div>
                </div>

            </div>
        </div>
    </section>

</main><!-- End #main -->

