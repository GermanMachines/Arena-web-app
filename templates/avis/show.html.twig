{% extends 'base-admin.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}

    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Score</th>
                <td>{{ avi.score }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ avi.commentaire }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ avi.idutulisateur.nom }}</td>
            </tr>
            <tr>
                <th>Jeux</th>
                <td>{{ avi.idjeux.nomjeux }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avis_index') }}" class="btn btn-secondary" class="btn btn-primary">back to list</a>

    <br>
    <br>

    {{ include('avis/_delete_form.html.twig') }}
{% endblock %}
