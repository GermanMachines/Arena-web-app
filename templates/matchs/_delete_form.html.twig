<form method="post" action="{{ path('app_matchs_delete', {'idmatch': match.idmatch}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ match.idmatch) }}">
    <button class="btn">Delete</button>
</form>
